[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "almasix-conduit"
version = "0.2.1"
description = "Reactive server-driven components for Almasix (almasix.conduit)"
requires-python = ">=3.11"
dependencies = [
    "almasix>=0.6.0",
]
readme = "README.md"
license = "MIT"
authors = [{ name = "Almasix Contributors" }]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.24.0",
    "httpx>=0.27.0",
    "ruff==0.16.6",
]

[project.entry-points."almasix.providers"]
conduit = "almasix.conduit.provider:ConduitServiceProvider"

[project.urls]
Homepage = "https://github.com/almasix-dev/almasix-conduit"
Repository = "https://github.com/almasix-dev/almasix-conduit"
Documentation = "https://conduit.almasix.com/"
Issues = "https://github.com/almasix-dev/almasix-conduit/issues"

[tool.hatch.build.targets.wheel]
packages = ["src/almasix"]

[tool.hatch.build.targets.sdist]
include = [
    "/src/almasix",
    "/tests",
    "/README.md",
    "/LICENSE",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["src"]
python_files = ["test_*.py", "*_test.py"]

[tool.ruff]
target-version = "py311"
line-length = 100
src = ["src", "tests"]

[tool.ruff.lint]
select = [
    "E4", "E7", "E9",
    "F",
    "I",
    "UP",
    "B",
    "RUF100",
]
ignore = ["E731", "UP042", "B008"]

[tool.ruff.lint.per-file-ignores]
"tests/**" = ["F811", "B017", "B018"]

[tool.ruff.format]
quote-style = "double"
